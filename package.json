{
  "name": "brainpod-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:orchestra": "python3 -m venv apps/orchestra/.venv && . apps/orchestra/.venv/bin/activate && pip install -r apps/orchestra/requirements.txt && cd apps/orchestra && uvicorn main:app --reload --port 8000",
    "build:web": "npm run build --workspace=apps/web"
  }
}
